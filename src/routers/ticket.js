import { Router } from "express";
import { z } from "zod"
import { ValidateQueryString } from "../middlewares/datavalidation";

export const TicketRouter = Router()

TrainRouter.param("uuid", )
TicketRouter.get("/:uuid", (req, res, next, value) => {
    
})